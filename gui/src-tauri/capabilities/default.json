{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the jobowalls picker window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
